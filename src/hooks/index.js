/**
 * Barrel export for all custom hooks
 * Makes it easier to import multiple hooks: import { useData, useSEO } from '@/hooks'
 */

export { useData } from './useData';
export { useSEO } from './useSEO';
